# mkdocs build output
site/

# Python / uv
.venv/
__pycache__/
*.pyc

# IDE
.vscode/
.idea/

# macOS
.DS_Store

# C++ build artefacts (added in v2 Sprint 1). Anchored to repo root so it
# does NOT also ignore scripts/build/ (the build-scripts subfolder added
# when scripts/ was organized by domain).
/build/
.pio/
*.o
*.obj

# Persisted device state (Sprint 5 PalFs PC root, Sprint 6 lighting state)
state/

# ESP32 LittleFS data partition source: real WiFi credentials live here and
# are uploaded via `pio run -t uploadfs`. Only the .example template is
# committed. See docs/deploy.md → "WiFi provisioning".
data/wifi.json

# scripts/moondeck.py persisted state: discovered devices + subnet for the Live
# Tests tab. Contains local-network IPs/MACs that are dev-host-specific —
# not portable across contributors. Auto-created on first launch.
moondeck.json
