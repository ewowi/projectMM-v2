# Lights

The first domain on top of the runtime. Effects produce frames, drivers consume them, layers and modifiers transform them in between. All of this is built on top of the core `Module` contract; the runtime itself has no knowledge of pixels.

This page is filled in during [Release 1 Sprint 4](development/release-01.md#sprint-4).
